- content_for :page_header_slim
  = render Global::PageHeader.new(t(:'admin.lti_providers.new.headline'), subtitle: t(:'admin.page_header'), type: :slim)

.container
  .row
    .col-md-12.mt20
      = render 'admin/lti_providers/form', provider: @provider
