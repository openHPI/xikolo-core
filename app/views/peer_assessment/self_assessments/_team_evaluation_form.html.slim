h4 = "#{index + 1}. #{team_evaluation_review.submission_user.name}"
hr.divider.slim
- @team_evaluation_rubrics.each do |rubric|
  = render 'team_evaluation_rating_form', review: team_evaluation_review, rubric: rubric, form: form
