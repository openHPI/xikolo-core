name: Set up bun
description: Set up Bun and install dependencies

runs:
  using: composite
  steps:
    - uses: oven-sh/setup-bun@v2
      with:
        bun-version-file: ".bun-version"
    - shell: sh
      run: bun install --immutable --refresh-lockfile
